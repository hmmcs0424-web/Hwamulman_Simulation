{
  "scripts": {
    "build": "node scripts/generate-image-manifest.js && node scripts/build-vercel-blob-client.js",
    "serve": "node scripts/static-server.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
